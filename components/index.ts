// ? Aquest arxiu es denomina "barril"
// ? Aqui colocarem totes les exportacions que volguem tenir a la carpeta de components 

export * from './navbar/Navbar';

// Client Components
export { ActiveLink } from './active-link/ActiveLink';